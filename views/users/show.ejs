<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Document</title>
</head>
<body>
    <main>
        <h1 class="title"><%= selected.username %></h1>
        <div class="photo-container">
            <div class="all-user-photos">
                <% selected.photos.forEach((element,index)=> { %>
                    <img class="user-photo-thumb" id="photo<%= index %>" src="<%= element.url %>" alt="element.about">
                <% }) %>
            </div>
            <div class="featured-user-photo">
                <img class="user-photo-feat" id="" src="" alt="">
            </div>
        </div>
    </main>
    <%- include('../partials/nav') %>
</body>
</html>